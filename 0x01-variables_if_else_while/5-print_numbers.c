#include <stdio.h>

/**
 * main - print all single digit of base 10 starting from o
 *
 * Return: Always return 0 (succes)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d\n", num);

	return (0);

}
